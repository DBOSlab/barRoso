% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std_types.R
\name{std_types}
\alias{std_types}
\title{Standardize and Fill Missing Type Status Information}
\usage{
std_types(df = NULL,
          colname_typeStatus = "typeStatus",
          rm_original_column = TRUE)
}
\arguments{
\item{df}{A data frame containing type designation records.}

\item{colname_typeStatus}{Name of the column holding type status information (default: \code{"typeStatus"}).}

\item{rm_original_column}{Logical; if \code{TRUE}, the original column is removed after cleaning (default: \code{TRUE}).}
}
\value{
A data frame with a standardized \code{typeStatus} column.
If \code{rm_original_column = FALSE}, the original values are preserved in a column named \code{typeStatusOriginal}.
}
\description{
Cleans and standardizes the \code{typeStatus} column in biodiversity datasets,
addressing inconsistencies in type designations. It removes irrelevant entries,
harmonizes formatting, and optionally fills missing values if present in other
duplicate records (assumed to be handled outside this function).
}
\details{
This function is used internally in the \code{barroso} package to prepare
type status data for reconciliation and label generation. It corrects known
placeholder or non-type entries (e.g. “Fotografia do Tipo”, “NOTATYPE”, “Epítipo”)
and simplifies terms like \code{"sim -"} to ensure clean type labels.
}
\examples{
\dontrun{
df <- read.csv("specimens.csv")
df_clean <- std_types(df,
                      colname_typeStatus = "tipo",
                      rm_original_column = FALSE)
}

}
\author{
Domingos Cardoso
}
