% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std_place.R
\name{std_place}
\alias{std_place}
\title{Standardize Place-Related Columns in Biodiversity Data}
\usage{
std_place(df = NULL,
          colname_continent = "continent",
          colname_country = "country",
          colname_stateProvince = "stateProvince",
          colname_county = "county",
          colname_municipality = "municipality",
          colname_locality = "locality",
          rm_original_column = TRUE)
}
\arguments{
\item{df}{A data frame containing biodiversity records.}

\item{colname_continent}{Column name for continent (default: \code{"continent"}).}

\item{colname_country}{Column name for country (default: \code{"country"}).}

\item{colname_stateProvince}{Column name for state or province (default: \code{"stateProvince"}).}

\item{colname_county}{Column name for county (default: \code{"county"}).}

\item{colname_municipality}{Column name for municipality (default: \code{"municipality"}).}

\item{colname_locality}{Column name for locality (default: \code{"locality"}).}

\item{rm_original_column}{Logical; if \code{TRUE}, original columns are removed
after cleaning (default: \code{TRUE}).}
}
\value{
A data frame with standardized geographic information. If
\code{rm_original_column = FALSE}, the original columns are retained with \verb{*Original}
suffixes.
}
\description{
Cleans and standardizes the geographic columns of a biodiversity collection
dataset. This includes unifying column names and harmonizing values for
\code{continent}, \code{country}, \code{stateProvince}, \code{county}, \code{municipality}, and \code{locality}.
The function handles translations, synonyms, upper-case anomalies, ISO country
codes, and common geographic aliases.
}
\details{
This function is used internally by the \code{barroso} package to support record
reconciliation, duplicate detection, and label generation across different
biodiversity databases. It ensures consistency of location fields by correcting
common mistakes and variations. Country names are translated to English and
harmonized using \code{countrycode} Brazilian and U.S. state abbreviations are
expanded to full names.
}
\examples{
\dontrun{
df <- read.csv("herbarium_records.csv")
df_clean <- std_place(df,
                      colname_country = "pais",
                      colname_stateProvince = "estado",
                      rm_original_column = FALSE)
}

}
\author{
Domingos Cardoso
}
